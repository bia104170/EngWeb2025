extends layout

block content
    .w3-card-4
        header.w3-container.w3-teal
            h1 Eliminar Filme

        p Tem certeza que pretende eliminar o filme: #{filme.id}?

        form(method='POST')
            button.w3-button.w3-teal(type='submit') Confirmar
            a(href='/filmes' class="w3-button w3-round") Cancelar
